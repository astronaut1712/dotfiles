---
phase: 02-dotfiles-configuration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - home/dot_config/nvim/init.lua
  - home/.chezmoiignore
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02

must_haves:
  truths:
    - "chezmoi apply places init.lua at ~/.config/nvim/init.lua"
    - "Neovim starts and lazy.nvim bootstraps plugins on first run"
    - "chezmoi does NOT overwrite lazy-lock.json or plugin dirs"
  artifacts:
    - path: "home/dot_config/nvim/init.lua"
      provides: "Neovim init file with lazy.nvim bootstrap"
      contains: "lazy.nvim"
    - path: "home/.chezmoiignore"
      provides: "Ignore rules protecting nvim plugin state"
      contains: "nvim/lazy-lock.json"
  key_links:
    - from: "home/dot_config/nvim/init.lua"
      to: "~/.config/nvim/init.lua"
      via: "chezmoi apply"
      pattern: "dot_config/nvim/init.lua"
    - from: "home/dot_config/nvim/init.lua"
      to: "lazy.nvim"
      via: "git clone bootstrap"
      pattern: "lazy.nvim.git"
---

<objective>
Migrate the Neovim init file into chezmoi management. Per EDIT-01, chezmoi manages ONLY `~/.config/nvim/init.lua` — all other nvim files (plugin configs, NvChad modules) are user-managed outside chezmoi. The init file must bootstrap lazy.nvim (EDIT-02).

Purpose: Neovim config entry point under version control, while lazy.nvim handles all plugin management independently.
Output: `home/dot_config/nvim/init.lua` + updated `.chezmoiignore`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.config/nvim/init.lua
@home/.chezmoiignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chezmoi-managed nvim init.lua</name>
  <files>home/dot_config/nvim/init.lua</files>
  <action>
Create the chezmoi source directory structure and copy the existing nvim init file:

1. Create directory `home/dot_config/nvim/` (chezmoi maps `dot_config` → `.config`).
2. Copy `.config/nvim/init.lua` (40 lines) verbatim to `home/dot_config/nvim/init.lua`. The file already contains the lazy.nvim bootstrap logic (git clone if not present), NvChad setup, and plugin loading. Do NOT modify it — it works as-is.

The existing `.chezmoiignore` already protects `lazy-lock.json` and plugin dirs. Verify this is still correct and add any missing ignores for the nvim directory.
  </action>
  <verify>
    <automated>diff /Users/quangnguyen/dotfiles/.config/nvim/init.lua /Users/quangnguyen/dotfiles/home/dot_config/nvim/init.lua && echo "Files match" || echo "Files differ"</automated>
  </verify>
  <done>
    - `home/dot_config/nvim/init.lua` exists and matches `.config/nvim/init.lua`
    - File contains lazy.nvim bootstrap (git clone block)
    - `.chezmoiignore` protects nvim/lazy-lock.json and plugin dirs
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .chezmoiignore for nvim protection</name>
  <files>home/.chezmoiignore</files>
  <action>
Verify and update `home/.chezmoiignore` to ensure chezmoi does NOT touch any nvim files except `init.lua`. The current ignore file already has:
```
.config/nvim/lazy-lock.json
.config/nvim/plugin/
.config/nvim/pack/
```

Add these additional ignores to be thorough:
```
.config/nvim/lua/
.config/nvim/.stylua.toml
```

This ensures chezmoi only manages `init.lua` and doesn't interfere with NvChad's `lua/` config tree or formatting config.
  </action>
  <verify>
    <automated>grep -c "nvim" /Users/quangnguyen/dotfiles/home/.chezmoiignore</automated>
  </verify>
  <done>
    - `.chezmoiignore` has ignore rules for `nvim/lua/`, `nvim/.stylua.toml`, `nvim/lazy-lock.json`, `nvim/plugin/`, `nvim/pack/`
    - Only `init.lua` would be deployed by chezmoi in the nvim directory
  </done>
</task>

</tasks>

<verification>
- `home/dot_config/nvim/init.lua` is identical to `.config/nvim/init.lua`
- `.chezmoiignore` blocks all nvim files except init.lua
- `chezmoi managed --include=files | grep nvim` shows only `init.lua`
</verification>

<success_criteria>
Neovim init file is under chezmoi management. lazy.nvim bootstrap is preserved. All other nvim state (plugins, configs, lock files) is protected from chezmoi overwrites.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dotfiles-configuration/02-03-SUMMARY.md`
</output>
