---
phase: 02-dotfiles-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - home/dot_zshrc.tmpl
autonomous: true
requirements:
  - SHLL-01
  - SHLL-02
  - SHLL-04
  - RTMG-03

must_haves:
  truths:
    - "chezmoi apply places a working .zshrc at ~/.zshrc"
    - "New shell loads Oh-My-Zsh, Powerlevel10k theme, and zsh-autosuggestions"
    - "mise is activated in zsh (go version and node --version work)"
    - "fzf keybindings respond (Ctrl+R, Ctrl+T, Alt+C)"
    - ".zshrc uses OS-conditional blocks for macOS vs Linux differences"
  artifacts:
    - path: "home/dot_zshrc.tmpl"
      provides: "Chezmoi-templated zsh config with OS conditionals"
      contains: "{{ if eq .chezmoi.os"
  key_links:
    - from: "home/dot_zshrc.tmpl"
      to: "~/.zshrc"
      via: "chezmoi apply"
      pattern: "dot_zshrc.tmpl"
    - from: "home/dot_zshrc.tmpl"
      to: "mise"
      via: "eval \"$(mise activate zsh)\""
      pattern: "mise activate zsh"
    - from: "home/dot_zshrc.tmpl"
      to: "fzf"
      via: "fzf keybinding source"
      pattern: "fzf"
---

<objective>
Migrate the existing `.zshrc` (175 lines) into a chezmoi-managed template at `home/dot_zshrc.tmpl`. The template must activate mise, load Oh-My-Zsh with Powerlevel10k, wire fzf keybindings, and use Go template conditionals for macOS vs Linux differences (Homebrew paths, platform-specific tools).

Purpose: This is the core shell config — everything else (nvim, wezterm, tmux) depends on a working shell.
Output: `home/dot_zshrc.tmpl` — a clean, well-structured zsh config template.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@home/.chezmoi.toml.tmpl
@home/.chezmoiexternal.toml
@home/.chezmoiignore
@.zshrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dot_zshrc.tmpl from existing .zshrc</name>
  <files>home/dot_zshrc.tmpl</files>
  <action>
Create `home/dot_zshrc.tmpl` by rewriting the existing `.zshrc` (175 lines) as a chezmoi Go template. Structure the file in these sections, in order:

1. **P10k instant prompt** — keep the existing instant prompt block at the top (required by P10k).

2. **Environment** — `LC_ALL`, `LC_CTYPE`, editor, locale exports.

3. **PATH setup with OS conditionals** — Use `{{ if eq .chezmoi.os "darwin" }}` / `{{ else }}` / `{{ end }}` blocks:
   - macOS: `/opt/homebrew/bin`, `/opt/homebrew/sbin` prepended to PATH (Apple Silicon Homebrew).
   - Linux: no Homebrew paths.
   - Common: `$HOME/go/bin`, `$HOME/.cargo/bin`, `$HOME/bin`.
   - REMOVE legacy cruft: nvm paths, rbenv, pew, yarn global, libxslt, libtool, llvm, make gnubin, openjdk@11 — these are replaced by mise.

4. **Oh-My-Zsh config** — `ZSH="$HOME/.oh-my-zsh"`, `ZSH_THEME="powerlevel10k/powerlevel10k"`, plugins list. Trim the plugins list to what's actually useful: `(git ssh-agent zsh-autosuggestions tmux docker)`. Remove kubectl/helm/tmuxinator/zsh-kubectl-prompt unless they're in Brewfile. Set `ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=240'`. Source `$ZSH/oh-my-zsh.sh`.

5. **mise activation** — `eval "$(mise activate zsh)"` (per RTMG-03). This replaces nvm, rbenv, goenv, etc.

6. **fzf integration** (per SHLL-02) — Source fzf keybindings and completion. Use OS conditional:
   - macOS: `source /opt/homebrew/opt/fzf/shell/key-bindings.zsh` and `source /opt/homebrew/opt/fzf/shell/completion.zsh`
   - Linux: `source /usr/share/doc/fzf/examples/key-bindings.zsh` and `source /usr/share/fzf/completion.zsh` (with existence checks)
   This enables Ctrl+R (history), Ctrl+T (file finder), Alt+C (cd).

7. **Aliases** — Keep useful aliases from existing .zshrc: `lg='lazygit'`, `lc='lsd'`, `weather=...`. Remove aliases for tools not in Brewfile (kubeseal, gk). Add alias `cm='chezmoi'` for convenience.

8. **P10k source** — `[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh` at the bottom.

REMOVE entirely from the migration:
- NVM block (replaced by mise)
- rbenv init (replaced by mise)
- pew shell_config (not used)
- JAVA_HOME (will be managed by mise if needed)
- Google Cloud SDK blocks (install via mise or Brewfile if needed)
- WAN_IP curl on every shell start (slow, unnecessary)
- Touchbar/iTerm2 integration (using WezTerm now)
- All commented-out blocks

Use `{{ if eq .chezmoi.os "darwin" }}` for macOS-only blocks and `{{ if eq .chezmoi.os "linux" }}` for Linux-only blocks. The template syntax must be valid chezmoi Go templates.

Do NOT use `{{ .chezmoi.os }}` in comments — use `{{/* comment */}}` for template comments.
  </action>
  <verify>
    <automated>cd /Users/quangnguyen/dotfiles && chezmoi execute-template < home/dot_zshrc.tmpl > /dev/null 2>&1 && echo "Template valid" || echo "Template INVALID"</automated>
  </verify>
  <done>
    - `home/dot_zshrc.tmpl` exists and is a valid chezmoi Go template
    - Template renders without errors via `chezmoi execute-template`
    - Contains `mise activate zsh` line
    - Contains fzf keybinding sources with OS conditionals
    - Contains `{{ if eq .chezmoi.os "darwin" }}` blocks for macOS paths
    - No legacy cruft (nvm, rbenv, pew, JAVA_HOME, WAN_IP)
    - P10k instant prompt at top, P10k source at bottom
  </done>
</task>

</tasks>

<verification>
- `chezmoi execute-template < home/dot_zshrc.tmpl` renders valid shell script
- Rendered output contains `eval "$(mise activate zsh)"`
- Rendered output contains fzf keybinding source lines
- Rendered output does NOT contain `nvm`, `rbenv`, `pew`, `JAVA_HOME`
- `shellcheck` on rendered output passes (or only cosmetic warnings)
</verification>

<success_criteria>
`home/dot_zshrc.tmpl` is a clean, well-structured chezmoi template that produces a working `.zshrc` with mise activation, fzf keybindings, Oh-My-Zsh, and OS-conditional PATH blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dotfiles-configuration/02-01-SUMMARY.md`
</output>
