---
phase: 02-dotfiles-configuration
plan: 05
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - home/.chezmoi.toml.tmpl
autonomous: true
requirements:
  - SCRT-01
  - SCRT-02

must_haves:
  truths:
    - "1Password CLI token retrieval is configured in chezmoi config"
    - "No plaintext secrets exist in any chezmoi source file"
    - "chezmoi diff shows no secrets in rendered output that shouldn't be there"
  artifacts:
    - path: "home/.chezmoi.toml.tmpl"
      provides: "Chezmoi config with 1Password integration"
      contains: "onepassword"
  key_links:
    - from: "home/.chezmoi.toml.tmpl"
      to: "1Password CLI"
      via: "op_available check and [onepassword] config"
      pattern: "lookPath.*op"
---

<objective>
Verify and harden 1Password integration in chezmoi config. Phase 1 already set up `op_available` detection in `.chezmoi.toml.tmpl`. This plan ensures the 1Password integration is ready for secret retrieval and no secrets have leaked into source files.

Purpose: Secrets management — ensure git repo contains zero plaintext secrets and chezmoi can retrieve secrets from 1Password at apply time.
Output: Updated `.chezmoi.toml.tmpl` if needed, clean secret audit.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@home/.chezmoi.toml.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify 1Password integration and audit for secrets</name>
  <files>home/.chezmoi.toml.tmpl</files>
  <action>
1. **Verify existing 1Password config** — `home/.chezmoi.toml.tmpl` already has:
   - `op_available` detection via `lookPath "op"`
   - `[onepassword]` section with `command = "op"`
   Confirm this is correct per chezmoi docs. If the `[onepassword]` section needs `prompt = false` or other flags, add them.

2. **Add a 1Password example comment** — Add a Go template comment block at the bottom of the file showing how to retrieve a secret:
   ```
   {{/* Example: use 1Password in templates:
        {{ if .op_available }}
        {{ onepasswordRead "op://vault/item/field" }}
        {{ end }}
   */}}
   ```
   This gives future templates a copy-paste pattern.

3. **Secret audit** — Scan ALL files in `home/` for common secret patterns:
   - Run: `grep -rn 'password\|secret\|token\|api.key\|private.key\|ssh_key' home/` (case insensitive)
   - Run: `grep -rn 'sk_live\|pk_live\|ghp_\|gho_\|AKIA' home/`
   - Any matches must be false positives (variable names, comments) or template references to 1Password.
   - If real secrets found, replace with `{{ onepasswordRead "op://vault/item/field" }}` template calls guarded by `{{ if .op_available }}`.

4. **Ensure .chezmoiignore includes sensitive files** — verify `.env`, `.env.local`, and any SSH key patterns are in the ignore list if not already.
  </action>
  <verify>
    <automated>cd /Users/quangnguyen/dotfiles && grep -rin 'sk_live\|pk_live\|ghp_\|gho_\|AKIA\|BEGIN.*PRIVATE' home/ | grep -v '{{' | grep -v '#' | wc -l</automated>
  </verify>
  <done>
    - `.chezmoi.toml.tmpl` has working 1Password config with `op_available` guard
    - Zero real secrets found in `home/` directory (grep returns 0 matches)
    - Example comment in config shows how to use `onepasswordRead`
    - `.chezmoiignore` protects sensitive file patterns
  </done>
</task>

</tasks>

<verification>
- `chezmoi execute-template < home/.chezmoi.toml.tmpl` renders valid TOML
- Secret audit grep returns 0 real matches
- Config has `[onepassword]` section with `command = "op"`
- Config has `op_available` boolean based on `lookPath "op"`
</verification>

<success_criteria>
1Password integration is verified and documented. Secret audit confirms zero plaintext secrets in chezmoi source. Future templates have a pattern to follow for secret retrieval.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dotfiles-configuration/02-05-SUMMARY.md`
</output>
