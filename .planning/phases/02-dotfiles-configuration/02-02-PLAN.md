---
phase: 02-dotfiles-configuration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - home/dot_p10k.zsh
autonomous: true
requirements:
  - SHLL-03

must_haves:
  truths:
    - "chezmoi apply places a working .p10k.zsh at ~/.p10k.zsh"
    - "Powerlevel10k prompt renders correctly in a new shell"
  artifacts:
    - path: "home/dot_p10k.zsh"
      provides: "Powerlevel10k configuration file"
      contains: "POWERLEVEL9K"
  key_links:
    - from: "home/dot_p10k.zsh"
      to: "~/.p10k.zsh"
      via: "chezmoi apply"
      pattern: "dot_p10k.zsh"
    - from: "home/dot_zshrc.tmpl"
      to: "home/dot_p10k.zsh"
      via: "source ~/.p10k.zsh"
      pattern: "p10k.zsh"
---

<objective>
Add the Powerlevel10k configuration file to chezmoi management. Since no `.p10k.zsh` exists in the repo, capture the user's live config from `~/.p10k.zsh` (generated by `p10k configure`) and place it at `home/dot_p10k.zsh`.

Purpose: P10k prompt appearance is defined by this file. Without it, P10k falls back to the configuration wizard on every new shell.
Output: `home/dot_p10k.zsh` — the P10k config managed by chezmoi.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture P10k config into chezmoi source</name>
  <files>home/dot_p10k.zsh</files>
  <action>
Copy the user's live Powerlevel10k config from `~/.p10k.zsh` into the chezmoi source directory at `home/dot_p10k.zsh`.

Steps:
1. Check if `~/.p10k.zsh` exists. If it does, copy it to `home/dot_p10k.zsh`.
2. If `~/.p10k.zsh` does NOT exist, create a minimal P10k config stub that:
   - Sets `POWERLEVEL9K_MODE='nerdfont-v3'`
   - Sets left prompt elements: `(os_icon dir vcs)`
   - Sets right prompt elements: `(status command_execution_time background_jobs)`
   - Sets `POWERLEVEL9K_PROMPT_ADD_NEWLINE=true`
   - Includes a comment: `# Run 'p10k configure' to regenerate this file with your preferences.`

The file is a plain shell file (not a template) since P10k config has no OS-specific differences.
  </action>
  <verify>
    <automated>test -f /Users/quangnguyen/dotfiles/home/dot_p10k.zsh && grep -q "POWERLEVEL9K" /Users/quangnguyen/dotfiles/home/dot_p10k.zsh && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>
    - `home/dot_p10k.zsh` exists in chezmoi source
    - File contains POWERLEVEL9K configuration variables
    - chezmoi will deploy it to `~/.p10k.zsh`
  </done>
</task>

</tasks>

<verification>
- `home/dot_p10k.zsh` exists and contains `POWERLEVEL9K` settings
- `chezmoi managed` shows `.p10k.zsh` in the managed files list
</verification>

<success_criteria>
Powerlevel10k config is under chezmoi management. `chezmoi apply` deploys it to `~/.p10k.zsh`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dotfiles-configuration/02-02-SUMMARY.md`
</output>
